import 'package:flutter/material.dart';
 
abstract final class MeetColors {
  // Backgrounds
  static const bg       = Color(0xFFECEDF6);
  static const cardBg   = Color(0xFFF3F4FB);
  static const surface  = Colors.white;
 
  // Brand
  static const primary  = Color(0xFF3B4B8C);
 
  // Text
  static const dark     = Color(0xFF1A1A2E);
  static const mid      = Color(0xFF4B5160);
  static const light    = Color(0xFF8A8FA8);
 
  // UI
  static const divider  = Color(0xFFE4E6F0);
  static const dotOn    = Color(0xFF3B4B8C);
  static const dotOff   = Color(0xFFCDD0E0);
 
  // FABs (home screen)
  static const fabJoin  = Color(0xFFDDE1F0);  // soft blue-lavender
  static const fabAcct  = Color(0xFFF5D6E8);  // soft pink
}
 